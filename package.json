{
  "name": "fonoster-website",
  "version": "0.0.1",
  "private": true,
  "description": "Fonoster main website",
  "license": "MIT",
  "scripts": {
    "build": "bash build.sh",
    "deploy": "npm run build && firebase deploy --only hosting",
    "emulate": "firebase emulators:start"
  }
}
